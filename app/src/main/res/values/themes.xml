<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BareksaTest" parent="Theme.MaterialComponents.Light">
        <!-- Custom action bar theme -->
        <item name="actionBarTheme">@style/whiteActionBar</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/surface_light</item>
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="textAppearanceHeadline6">@style/headline6</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_up_arrow</item>
    </style>
</resources>